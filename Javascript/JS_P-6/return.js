function sum(a, b) {
  return a + b;
}

console.log(sum(2, 98));

//==============================

function isAdult(age) {
  if (age >= 18) {
    return "adult";
  } else {
    return "not adult";
  }
}

console.log(isAdult(22));
//=============================== 

//? slice method which follow same as return keyword do.
let arr = [2, 3, 4, 5];
// arr.slice(2,4);
